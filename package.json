{
  "name": "snaptext",
  "version": "1.0.0",
  "description": "Chrome Extension – Extract text from images using local OCR",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "setup": "npm install && npm run build"
  },
  "dependencies": {
    "tesseract.js": "^5.0.0"
  }
}
